// Stub implementation for non-web platforms. The real implementation is
// conditionally imported when running on web (dart.library.html).
/// Stub implementation for non-web platforms. The real implementation is
/// conditionally imported when running on web (dart.library.html).
void openHtmlInNewTab(String htmlContent, {bool autoPrint = false}) {
  // no-op on non-web platforms
}
